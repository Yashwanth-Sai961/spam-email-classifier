"""
src package — Email Security Assistant core pipeline modules.

This file's only job is to mark `src/` as a Python package so that
`from src.preprocess import preprocess_text` and similar imports work
regardless of the current working directory.
"""
